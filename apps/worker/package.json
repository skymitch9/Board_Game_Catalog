{
  "name": "@bgc/worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Cloudflare Worker: HTTP routing and auth. All logic lives in packages/.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:games2": "wrangler deploy --env games2",
    "db:migrate:local": "wrangler d1 migrations apply board-game-catalog --local",
    "db:migrate": "wrangler d1 migrations apply board-game-catalog --remote",
    "db:migrate:games2": "wrangler d1 migrations apply board-game-catalog-2nd --remote --env games2",
    "tail": "wrangler tail",
    "tail:games2": "wrangler tail --env games2",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@bgc/bgg": "*",
    "@bgc/core": "*",
    "@bgc/db": "*",
    "hono": "^4.6.14",
    "jose": "^5.9.6",
    "zod": "^3.24.1",
    "@bgc/barcode": "*"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250109.0"
  }
}
